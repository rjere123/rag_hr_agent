# Extra dependencies needed ONLY to (re)build the vector store locally:
#     python ingest.py        # PDF -> chunks -> ChromaDB
#     python create_pdf.py    # regenerate the sample HR policy PDF
#
# The deployed Streamlit app does NOT need these — the vector-store/ directory
# is committed to the repo. Install with:
#     pip install -r requirements-ingest.txt
-r requirements.txt
pdfplumber
langchain
langchain-text-splitters
sentence-transformers
reportlab
